#ifndef UTILS_CPP
#define UTILS_CPP

#include <iostream>

inline int add(int x, int y) {
    return x + y;
}

inline int dec(int x) {
    return --x;
}

inline bool cmp(int x, int y) {
    return (x == y);
}

inline int mul(int x, int y) {
    return x * y;
}

#endif